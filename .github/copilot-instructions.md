# Copilot Instructions

- This repository is a dependency-free Go clipboard and file server.
- Keep startup compatible with PowerShell, bash, and Termux bash.
- Prefer Go standard library and browser APIs before adding dependencies.
- Runtime clipboard/file data belongs under `data/` and must stay untracked.
- Keep UI changes practical and compact; this is a utility app, not a landing page.
